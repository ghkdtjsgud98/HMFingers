import speech from '@google-cloud/speech'
import fs from 'fs'

/* async function convertAudioToScript() {
  const client = new speech.SpeechClient();
  const file = './resources/audio.raw' // 없는 파일임.

  const file = fs.readFileSync(filename);
  const au
}
*/
